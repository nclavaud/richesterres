{% extends '::base_admin.html.twig' %}

{% block body -%}
    <h1>Renseigner une évaluation</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('admin_evaluation') }}">
            Revenir à la liste
        </a>
    </li>
</ul>
{% endblock %}
